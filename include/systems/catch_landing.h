#ifndef SYSTEM_CATCH_LANDING_H
#define SYSTEM_CATCH_LANDING_H

#include "../kodama_state.h"

// consider a cliff, where the player falls if they don't land?
void system_catch_landing(KodamaState * state);

#endif